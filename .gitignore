# Local build inputs and caches — never published.
# The board inlines every stat at build time, so nothing here is served at runtime.
data/
.venv/
__pycache__/
*.pyc
*.bak*
*.xlsx
*.xls
preview/

# Source tables consumed by build_depthcharts.py, not published assets
coaches*.csv
names_ko.csv

# Editor / OS cruft
.DS_Store
Thumbs.db
desktop.ini
*.swp
*~

# 로컬 편집 도구 — 사이트에 나가는 물건이 아닙니다.
# deploy.sh 는 이것들을 site/ 로 옮기지 않지만, 실수로 들어와도 커밋되지 않게
# 여기서 한 번 더 막습니다.
admin.py
collect_news.py
content/
